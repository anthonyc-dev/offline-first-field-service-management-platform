Dcoker Redis cli command:
      docker exec -it backend-redis-1 redis-cli
      KEYS * 
      ZCARD rate:172.18.0.1
      ZRANGE rate:172.18.0.1 0 -1 WITHSCORES 
       
     -View Audit log rate limit command
      LRANGE rate_limit_hits 0 9

     -Redis session command
      GET session:2d688679-128b-4d74-a1dd-c96b1a15af77
      SMEMBERS user_sessions:2f1f9010-8469-4a1c-8e44-532bb35f5803

      | Command                      | Purpose                                |
      | ---------------------------- | -------------------------------------- |
      | `TTL <key>`                  | Show remaining time in seconds         |
      | `PTTL <key>`                 | Show remaining time in milliseconds    |
      | `EXPIRE <key> <seconds>`     | Set expiration in seconds              |
      | `PEXPIRE <key> <ms>`         | Set expiration in milliseconds         |
      | `EXPIREAT <key> <timestamp>` | Set expiration at a specific Unix time |
      | `DEL <key>`                  | Delete the key immediately             |


      HGETALL bull:audit:1 
      GET bull:audit:id 
      GET bull:audit:stalled-check 
      HGETALL bull:audit:meta
      XREAD COUNT 10 STREAMS bull:audit:events 0

//TODO:✅
Next to do for Event / audit log
  - Elasticsearch/OpenSearch for searchable audit logs + Object Storage (S3) for immutable long-term audit history

//TODO:✅
Senior recommendation (secure default):
Revoke all refresh tokens for that user and all active sessions in Redis, force re-login, log and alert, then investigate the breach. Treat refresh token reuse as a full compromise.

//TODO:✅
NGINX
  Reverse proxy + load balancing
  Rate limiting (global + per-IP)
  Bot & abuse mitigation
  IP allow/deny
  Admin access via private network / VPN only
  Security hardening
  Attack surface reduction
  Structured logging
  Ready to drop into a real server