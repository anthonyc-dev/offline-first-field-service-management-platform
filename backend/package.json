{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "tsc",
    "start": "node dist/server.js",
    "docker:down": "docker-compose -f docker-compose.dev.yml down",
    "docker:build": "docker-compose -f docker-compose.dev.yml up -d --build",
    "docker:dev:restart": "docker-compose -f docker-compose.dev.yml down && docker-compose -f docker-compose.dev.yml up -d --build",
    "dev:up": "docker-compose -f docker-compose.dev.yml up -d --build",
    "dev:migrate": "docker-compose -f docker-compose.dev.yml exec api npx prisma migrate dev",
    "dev:studio": "docker-compose -f docker-compose.dev.yml exec api npx prisma studio --browser none",
    "dev:generate": "docker-compose -f docker-compose.dev.yml exec api npx prisma generate",
    "prod:up": "docker-compose -f docker-compose.prod.yml up -d --build",
    "prod:migrate": "docker-compose -f docker-compose.prod.yml exec api npx prisma migrate deploy",
    "prisma:migrate": "docker-compose -f docker-compose.dev.yml exec api npx prisma migrate dev",
    "prisma:generate": "docker-compose -f docker-compose.dev.yml exec api npx prisma generate",
    "prisma:studio": "docker-compose -f docker-compose.prod.yml exec api npx prisma studio --browser none"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10",
    "@types/node": "^25.0.3",
    "nodemon": "^3.1.11",
    "prisma": "^7.2.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
